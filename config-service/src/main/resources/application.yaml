server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/xxgiianxx/microservices-tutorial
          search-paths: config-data
  application:
    name: config-service